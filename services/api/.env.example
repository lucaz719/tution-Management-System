# PostgreSQL connection string used by Prisma
DATABASE_URL="postgresql://postgres:postgres@localhost:5432/tms?schema=public"

# REQUIRED — the API refuses to start without it. Generate a strong value, e.g.:
#   node -e "console.log(require('crypto').randomBytes(48).toString('base64url'))"
JWT_SECRET=""

# Optional overrides for the bootstrap seed (npm run db:seed).
# If SEED_ADMIN_PASSWORD is omitted, a random password is generated and
# printed to the console once on first seed.
SEED_ADMIN_EMAIL="superadmin@tms.local"
# SEED_ADMIN_PASSWORD=""

# White-label license: maximum number of client tenants that can be provisioned
# (internal platform/demo tenants excluded). Leave unset for unlimited.
WHITE_LABEL_TENANT_LIMIT="1"
